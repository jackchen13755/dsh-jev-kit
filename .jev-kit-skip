# 本仓库的语料**故意**由"像凭据的假文本"构成（隐私扫描夹具），
# 因此推送前扫描必须排除它们，否则它永远拦下自己的合法推送。
# 语法：git pathspec，一行一条，# 开头为注释。

# 生成物：由 src/ 编译而来，扫两份只会把同一处发现报两遍。
lib/

# 扫描器自身的定义：脚本里写着 `ghp_[A-Za-z0-9]{20,}` 这样的模式与示例，
# 属于"自己匹配自己"——与语料同理，扫它只会得到必然的假阳性。
scripts/

# 夹具与夹具的测试：内容按设计就是"像凭据的文本"。
src/corpus.ts
src/bench.ts
test/*.test.mjs

# 文档：会引用夹具原文与扫描模式作为示例。
README.md
