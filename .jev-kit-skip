# 本仓库的三类**必然**假阳性，以及排除它们的理由。
# 语法：git pathspec，一行一条，# 开头为注释。
#
# ① 生成物：由 src/ 编译而来，与源码同源，扫两份只会把同一处发现报两遍。
lib/
#
# ② 扫描器自身的定义：脚本与标定措辞里写着 `ghp_[A-Za-z0-9]{20,}`、
#    `.internal`、`/Users/dev` 这类模式与示例——它们必须存在，否则扫描器无法工作。
#    扫它们等于自己匹配自己，是必然的假阳性。
scripts/
src/channels.ts
#
# ③ 夹具：内容按设计就是"像凭据的文本"，标签就是它的真值。
src/corpus.ts
src/bench.ts
test/*.test.mjs
#
# ④ 文档：会引用夹具原文与扫描模式作为示例。
README.md

# ⑤ 本豁免文件自身：注释里必须引用那些模式。
.jev-kit-skip
